---
import { formatDate } from "@/utils/formats";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<div class="mt-12">
  <p class="text-sm font-medium">
    Published on <time datetime={date.toString()}><i>{formatDate(date)}</i></time>.
  </p>
</div>
